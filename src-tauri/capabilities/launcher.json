{
  "identifier": "launcher",
  "description": "Local shell pages (launcher + standalone settings window): core defaults (event listen, invoke, window) for the loading/error UI and proxy settings.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
