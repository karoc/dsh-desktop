# dsh Desktop overlay: inject the notification client plugin into the web
# profile's loader roster. Applied at runtime with `dsh web --patch <this>`;
# the official @deepseek-ai/dsh package is never modified, so auto-updating
# dsh to the latest npm release cannot wipe this plugin.
- insert:
    - id: desktop-notifications
      name: '@dsh-desktop/client-notifications'